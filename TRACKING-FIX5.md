# TRACKING-FIX5 — Corrección de 5 módulos marcados como "hecho" pero con implementación de juguete

| # | Módulo | Problema detectado | Estado | Nota / commit |
|---|--------|---------------------|--------|----------------|
| 1 | src/lib/sentry.ts | No importa @sentry/nextjs, solo hace console.log/error disfrazado de Sentry | hecho | Instalado @sentry/nextjs@10.67.0, creados sentry.client.config.ts y sentry.server.config.ts, src/lib/sentry.ts usa SDK real con fallback a console.log/logger cuando no hay DSN. Verificado: tsc --noEmit sin errores en el módulo. DSN debe configurarse via SENTRY_DSN (server) o NEXT_PUBLIC_SENTRY_DSN (client) en Vercel. |
| 2 | src/lib/api-key-auth.ts | Las API keys viven en un Map en memoria, no en la base de datos — en serverless (Vercel) se pierden entre instancias/cold starts | hecho | Agregado modelo ApiKey a prisma/schema.prisma (keyHash SHA-256, scopes JSON, expiresAt, revokedAt). api-key-auth.ts usa Prisma/DB: createApiKey, validateApiKey, revokeApiKey, listApiKeys, checkScope. Tests: 8/8 pass. Verificado: tsc --noEmit sin errores, vitest run 8/8. |
| 3 | src/lib/dead-mans-switch.ts | Estado en Map en memoria, sin persistencia, sin cron real, sin envío de email/notificación | hecho | Agregado modelo DeadManSwitch a Prisma (userId único, gracePeriodMs, lastPulseAt, notifyEmail). dead-mans-switch.ts: createSwitch, pulse, checkAllSwitches con persistencia real. Cron endpoint en /api/cron/dead-mans-switch para Vercel Cron. Email vía nodemailer/SMTP configurable (SMTP_HOST, SMTP_USER, SMTP_PASS) con fallback a log. Tests: 8/8 pass. |
| 4 | src/lib/onepassword-adapter.ts | Dice parsear .1pux (que es un ZIP) pero no descomprime nada — solo mapea JSON ya extraído a mano | hecho | Implementado parse1pux() con JSZip real: descomprime el ZIP, extrae export.data, parsea el JSON de 1Password (accounts/items/sections/fields), mapea categorías y URLs. itemsToSecrets() convierte a formato interno. Tests: 7/7 pass con .1pux construido en test. Verificado: tsc sin errores, vitest 7/7. |
| 5 | src/app/api/metrics/route.ts + metrics-store | Contadores en variables en memoria (let requestCount = 0), active_connections hardcodeado en 1 — no acumula nada real entre requests serverless | pendiente | |
