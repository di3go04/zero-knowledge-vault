{
  "name": "@zk-vault/cli",
  "version": "0.1.0",
  "description": "CLI tool for Zero-Knowledge Vault — manage secrets from the terminal.",
  "type": "module",
  "bin": {
    "zk-vault": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "scripts": {
    "start": "bun src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun --format esm --external '@zk-vault/*'",
    "postbuild": "bunx chmod +x dist/index.js 2>/dev/null || true"
  },
  "files": [
    "src/",
    "dist/",
    "README.md"
  ],
  "keywords": [
    "cli",
    "password-manager",
    "zero-knowledge",
    "encryption",
    "security"
  ],
  "author": "ZK Vault Team",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  }
}
