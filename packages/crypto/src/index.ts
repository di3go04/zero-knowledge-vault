/**
 * @zk-vault/crypto — Public API placeholder.
 *
 * This package is a thin wrapper around the canonical crypto module
 * at src/lib/crypto. When consumed from within the monorepo, the
 * root tsconfig path alias "@/*" resolves the imports.
 *
 * To consume the crypto module standalone (outside this monorepo),
 * copy the src/lib/crypto/ directory into your project.
 *
 * The actual exports are available via `import { ... } from "@/lib/crypto"`
 * in the main application.
 */
export {};
